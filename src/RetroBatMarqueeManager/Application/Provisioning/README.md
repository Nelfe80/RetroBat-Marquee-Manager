# Application/Provisioning

MarqueeLightingProvisioner — génération flux tendu du workspace `dynamic/<system>/<game>/` (CDC §4) :
SlugResolver, WorkspaceManager, ManifestService, MemCoverageClient, SceneAssembler,
CacheEvictionService, WarmupScheduler.
Implémentation : Phase 3.
